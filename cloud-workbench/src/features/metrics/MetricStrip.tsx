export function MetricStrip(){
  return (
    <div className="rounded-lg p-2 text-xs" style={{ background: 'var(--bg-tertiary)', color: 'var(--text-primary)' }}>
      Metrics: ρ Δ, POVM shift, fidelity, coherence (wired later)
    </div>
  );
}
