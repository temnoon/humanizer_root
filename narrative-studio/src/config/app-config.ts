export const APP_CONFIG = {
  // Auto-save settings
  autoSaveDebounceMs: 5000, // 5 seconds

  // UI settings
  defaultViewMode: 'split' as const,

  // Export settings
  enableZipExport: true,

  // Session settings
  autoCreateSession: true,
  sessionNamePrefix: 'Session'
};
